name: ci

# The gates AGENTS.md requires, run for real instead of by hand. Two of the
# defects found while developing 0.1.0 only surfaced because a default was
# flipped locally; CI is what makes that systematic.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Release and both sanitizer configurations, on both supported compilers.
  test:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        compiler: [g++, clang++]
        sanitize: [none, asan, tsan]
    name: ${{ matrix.compiler }} / ${{ matrix.sanitize }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install OpenSSL headers
        run: sudo apt-get update && sudo apt-get install -y libssl-dev
      - name: Configure
        run: |
          build_type=Release
          if [ "${{ matrix.sanitize }}" != none ]; then build_type=Debug; fi
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=$build_type \
            -DCMAKE_CXX_COMPILER=${{ matrix.compiler }} \
            -DCIO_SANITIZE=${{ matrix.sanitize }} \
            -DCIO_TLS=ON
      - name: Build
        run: cmake --build build -j
      - name: Test
        run: ctest --test-dir build --output-on-failure
      # A single pass can miss an intermittent lifetime bug; the leak that the
      # cancelled-connect work introduced reproduced in roughly half of runs.
      - name: Repeat the race-sensitive suites
        if: matrix.sanitize != 'tsan'
        run: |
          for i in $(seq 1 5); do
            ./build/test_net
            ./build/test_scoped
            ./build/test_dns
          done

  # Build variants that nothing else exercises: the shared library carries the
  # dual-symbol TLS ABI that only exists for libcio.so, and the metrics build is
  # the instrumentation every benchmark depends on. Both rot silently without a
  # job that compiles them.
  variants:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: shared
            flags: -DBUILD_SHARED_LIBS=ON
          - name: metrics
            flags: -DCIO_METRICS=ON
    name: variant / ${{ matrix.name }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release ${{ matrix.flags }}
          cmake --build build -j
          ctest --test-dir build --output-on-failure

  # The library claims Linux, not glibc-on-x86_64; an arm64 job keeps the claim
  # honest about at least the second architecture people actually run.
  arm64:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j
          ctest --test-dir build --output-on-failure

  # Short fuzz smoke over the parsers that consume untrusted bytes. Not a
  # campaign — a regression gate: a crash found by a longer local run lands in
  # fuzz/corpus and stays fixed forever.
  fuzz:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: sudo apt-get update && sudo apt-get install -y clang
      - run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug             -DCMAKE_CXX_COMPILER=clang++ -DCIO_SANITIZE=asan -DCIO_FUZZ=ON             -DCIO_BUILD_TESTS=OFF -DCIO_BUILD_EXAMPLES=OFF -DCIO_BUILD_BENCHMARKS=OFF
          cmake --build build -j
      - name: Run each fuzzer briefly
        run: |
          for f in build/fuzz_*; do
            echo "== $f"
            "$f" -max_total_time=45 -print_final_stats=1 fuzz/corpus/$(basename "$f") 2>&1 | tail -3
          done

  # A longer soak must not run under a sanitizer: ASan and TSan can stop
  # symmetric coroutine transfer from remaining a native tail call, so a
  # sanitizer soak is not a release-runtime stack test.
  soak:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - run: cmake --build build -j
      - run: ./build/test_soak 60

  # The library must stay installable and consumable via find_package.
  install:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: sudo apt-get update && sudo apt-get install -y libssl-dev
      - name: Install to a prefix
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="$PWD/prefix" -DCIO_TLS=ON
          cmake --build build -j
          cmake --install build
      - name: Consume it from a separate project
        run: |
          mkdir -p /tmp/downstream && cd /tmp/downstream
          cat > CMakeLists.txt <<'EOT'
          cmake_minimum_required(VERSION 3.20)
          project(downstream LANGUAGES CXX)
          find_package(cio 0.2.0 REQUIRED)
          add_executable(app main.cpp)
          target_link_libraries(app PRIVATE cio::cio cio::cio_tls)
          EOT
          cat > main.cpp <<'EOT'
          #include <cio/cio.hpp>
          #include <cio/tls.hpp>
          CIO_MAIN {
              static_assert(CIO_VERSION >= CIO_VERSION_NUMBER(0, 2, 0));
              co_return 0;
          }
          EOT
          cmake -S . -B b -DCMAKE_PREFIX_PATH="$GITHUB_WORKSPACE/prefix"
          cmake --build b -j
          ./b/app

  # add_subdirectory() consumers must not inherit tests, examples or installs.
  subdirectory:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: |
          mkdir -p /tmp/parent/vendor && cd /tmp/parent
          cp -r "$GITHUB_WORKSPACE" vendor/cio
          rm -rf vendor/cio/.git
          cat > CMakeLists.txt <<'EOT'
          cmake_minimum_required(VERSION 3.20)
          project(parent LANGUAGES CXX)
          add_subdirectory(vendor/cio)
          add_executable(app main.cpp)
          target_link_libraries(app PRIVATE cio::cio)
          EOT
          printf '#include <cio/cio.hpp>\nCIO_MAIN { co_return 0; }\n' > main.cpp
          cmake -S . -B b -DCMAKE_BUILD_TYPE=Release
          cmake --build b -j
          ./b/app

  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Check whitespace
        run: git diff --check HEAD~1 || true
      - name: Trailing whitespace and tabs
        run: |
          if git grep -nP '[ \t]+$' -- '*.hpp' '*.cpp'; then
            echo "trailing whitespace above"; exit 1
          fi
